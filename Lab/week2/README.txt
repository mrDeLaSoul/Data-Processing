for === to return true, the value must be similar as well as the datatypes of the values.
with == , in contrast to ===, the datatype does not have to be similar

a closure is a function that has acces to the variables that are declared in the parent(higher order) function

a higher order function is a function that has other function within its scope(as an argument or it returns a function).

a query selector in javascript is a method to get the element within a webpage to get the information that is stored within that element